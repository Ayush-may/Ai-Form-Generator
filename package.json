{
  "name": "form-generator-project",
  "version": "1.0.0",
  "description": "Root package to manage frontend and backend",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client2\" \"npm run start:dev --prefix server\"",
    "install:all": "npm install --prefix client && npm install --prefix server",
    "client": "npm run dev --prefix client",
    "server": "npm run start:dev --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}